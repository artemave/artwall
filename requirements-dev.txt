# Development-only tooling. The app itself has no runtime dependencies.
# Install with: pip install -r requirements-dev.txt
coverage>=7
mypy>=1.8
ruff>=0.4
# PyGObject-stubs (type stubs for the optional GTK overlay) needs a special
# from-source, Gtk3, --no-deps install — see the `install-dev` target in the Makefile.
